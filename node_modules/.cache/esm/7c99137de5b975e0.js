let idArg,queryType,stringArg;_e5b‍.x([["Query",()=>Query]]);_e5b‍.w("nexus",[["idArg",["idArg"],function(v){idArg=v}],["queryType",["queryType"],function(v){queryType=v}],["stringArg",["stringArg"],function(v){stringArg=v}]]);

       const Query = queryType({
  definition(t) {
    t.field('Stat', {
      type: 'Stats',
      nullable: true,
      args: { id: idArg() },
      resolve: (parent, { id }, ctx) => {
        return ctx.prisma.stats.findOne({
          where: {
            id,
          },
        })
      },
    })

    t.list.field('Stats', {
      type: 'Stats',
      resolve: (parent, arg, ctx) => {
        return ctx.prisma.stats.findMany()
      },
    })

    t.list.field('filterStat', {
      type: 'Stat',
      args: {
        searchString: stringArg({ nullable: true }),
      },
      resolve: (parent, { searchString }, ctx) => {
        return ctx.prisma.stat.findMany({
          where: {
            OR: [
              { name: { contains: searchString } },
              { KD: { contains: searchString } },
              { GamerTag: { contains: searchString } },
              { Platform: { contains: searchString } },
            ],
          },
        })
      },
    })
  },
})
