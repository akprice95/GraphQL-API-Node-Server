let PrismaClient,fs;_ad4‍.w("@prisma/client",[["PrismaClient",["PrismaClient"],function(v){PrismaClient=v}]]);_ad4‍.w("fs",[["default",["fs"],function(v){fs=v}]]);


const prismaClient = new PrismaClient()

const coD = fs.readFileSync('prisma/data/coD.json')

function loadStats() {
  const list = JSON.parse(coD)

  return list.map(stats => {
    return {
      data: {
        name: stats.name,
        KD: stats.KD,
        Platform: stats.Platform,
        GamerTag: stats.GamerTag,
      },
    }
  })
}

async function main() {
  try {
    const allStats = loadStats()
    for (let stats of allStats) {
      await prismaClient.stats
        .create(stats)
        .catch(err => console.error(`Error trying to generate stats `))
    }
  } catch (err) {
    _ad4‍.g.console.log(err)
  }
}

main()
  .catch(e => _ad4‍.g.console.error(e))
  .finally(async () => {
    await prismaClient.disconnect()
  })
